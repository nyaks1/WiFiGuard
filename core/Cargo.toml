[package]
name = "wifiguard_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
flutter_rust_bridge = "2"

[profile.release]
opt-level = "z"      # Optimize for size
lto = true
strip = true
